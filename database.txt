create database database_name;
use database_name;
create table table_name(var datatype,.....);

//CREATE TABLE EMP
CREATE TABLE EMP(EMPNO INT PRIMARY KEY, ENAME VARCHAR(20) NOT NULL,JOB VARCHAR(10) NOT NULL, DEPTNO INT, SAL FLOAT(7,2));


// ALTER TABLE ADD EXPERIENCE
ALTER TABLE EMP ADD COLUMN EXPERIENCE INT ;


//MODIFY COLUMN
ALTER TABLE EMP MODIFY COLUMN JOB VARCHAR(50);

// CREATE A NEW TABLE
CREATE TABLE DEBT(DEPTNO INT PRIMARY KEY, DNAME VARCHAR(10), LOC VARCHAR(10));

//CREATE TABLE  WITH ENANE AND EMPNO
CREATE TABLE EMP1 (SELECT EMPNO, ENAME FROM EMP);

//ADD CONSTRAINTS
ALTER TABLE EMP1 ADD CHECK (EMPNO < 100);

// DROP COLUMN EXPERIENCE TO THE EMP TABLE // DELETE
ALTER TABLE EMP DROP COLUMN EXPERIENCE;

// TRUNCATE THE EMP TABLE AND DROP THE DEPT TABLE // 
INSERT INTO EMP VALUES(1,0, 'DSHFH', 'DSHK',30399); // JOB IS INT CHANGE IT TO VARCHAR



